name: CI

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - "**/*.md"
      - "LICENSE"
      - ".gitignore"
  push:
    branches: [main]
    paths-ignore:
      - "**/*.md"
      - "LICENSE"
      - ".gitignore"
  workflow_dispatch:

jobs:
  ci:
    uses: cplieger/ci/.github/workflows/shell-ci.yaml@a4a5c8d560e69958c08f1294f699a9c41d187a23 # v1
    with:
      hadolint-ignores: "DL3018,DL3008"
      shellcheck-paths: "./*.sh"
