https://gitlab.alpinelinux.org/alpine/aports/-/tree/3.24-stable/main/krb5-conf version 1.0 (no license file published upstream; APKBUILD declares MIT) The aport has no source at all: its package() writes /etc/krb5.conf from a heredoc in the APKBUILD, so there is no upstream tarball and neither the aport directory nor the aports repository root publishes a license file. The url= field points at MIT Kerberos, whose code this package does not contain.
